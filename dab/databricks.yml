bundle:
  name: ml_exam_lifecycle_project

variables:
  catalog:
    default: main
  schema:
    default: ml_exam
  existing_cluster_id:
    description: Existing interactive cluster ID used by all job tasks (override at deploy time)
    default: 42be771719f14826

sync:
  paths:
    - .
    - ../scripts
    - ../src
    - ../pyproject.toml

resources:
  experiments:
    ml_exam_experiment:
      name: /Shared/ml_exam_project

  registered_models:
    exam_candidate_model:
      name: exam_candidate_model

  jobs:
    ml_lifecycle_job:
      name: ml-lifecycle-end-to-end
      tasks:
        - task_key: ingest_seed_data
          existing_cluster_id: ${var.existing_cluster_id}
          spark_python_task:
            python_file: ../scripts/run_data_ingestion.py

        - task_key: build_features
          existing_cluster_id: ${var.existing_cluster_id}
          depends_on:
            - task_key: ingest_seed_data
          spark_python_task:
            python_file: ../scripts/run_feature_pipeline.py

        - task_key: train_model
          existing_cluster_id: ${var.existing_cluster_id}
          depends_on:
            - task_key: build_features
          spark_python_task:
            python_file: ../scripts/run_training.py

        - task_key: monitor
          existing_cluster_id: ${var.existing_cluster_id}
          depends_on:
            - task_key: train_model
          spark_python_task:
            python_file: ../scripts/run_monitoring.py

        - task_key: retrain
          existing_cluster_id: ${var.existing_cluster_id}
          depends_on:
            - task_key: monitor
          spark_python_task:
            python_file: ../scripts/run_retraining.py

targets:
  dev:
    workspace:
      host: https://dbc-b576ddfe-9e99.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-b576ddfe-9e99.cloud.databricks.com
